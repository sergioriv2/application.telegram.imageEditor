export enum BotCommand {
  Start = 'start',
  ResizeImage = 'resizeimage',
  ConfirmResizeImage = 'confirmresize',
}

export enum BotCommandsDescription {
  Start = 'Empieza a interactuar con el bot',
  ResizeImage = 'Ajuste el tamaño una imagen a las dimensiones necesarias para subirlas al bot de sticker (@Stickers)',
  ConfirmResizeImage = 'Confirma el cambio de tamaño de la imagen',
}
