export enum BotCommand {
  Start = 'start',
  CropImage = 'cropimage',
}

export enum BotCommandsDescription {
  Start = 'Empieza a interactuar con el bot',
  CropImage = 'Recorta una imagen con las dimensiones para subirlas al bot de sticker',
}
